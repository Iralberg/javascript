// esse é o for padrao, nele vc define a variavel, depois a condição, e quantas vezes ele vai pular
// essa é a estrutura do for => for (let contador;contador <=1; contador ++)
for (let c=1;c<=10;c++){
    console.log(` Numero ${c} `)
}